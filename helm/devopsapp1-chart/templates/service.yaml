apiVersion: v1
kind: Service
metadata:
  name: devopsapp1-service
  labels:
    app: devopsapp1
spec:
  selector:
    app: devopsapp1
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000
  type: NodePort