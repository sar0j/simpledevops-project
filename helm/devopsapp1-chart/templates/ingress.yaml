apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devopsapp-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: devopsapp1.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devopsapp1-service
            port:
              number: 80